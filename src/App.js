import "./app.css";
import React from "react";
/*import callAPI from './service/callAPI';
import { useSelector } from "react-redux";
import { useDispatch } from "react-redux";*/

function App() {
  /*let token ="";
  return (
    <React.Fragment>
      {callAPI.login('tony@stark.com','password123')}
      {token = localStorage.getItem('token')}
      {console.log(callAPI.getUserProfile(token))}
      <p>test api</p>
    </React.Fragment>
  );
  let test = useSelector((state)=>state.firstName);
  let dispatch = useDispatch();
  dispatch({type: "login", payload:{firstName:"TestFirst", lastName:"TestLast", token:"aaaa"}})*/
  return (
    <React.Fragment>
    </React.Fragment>
  );
}

export default App;
